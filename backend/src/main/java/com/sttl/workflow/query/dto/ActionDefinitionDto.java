package com.sttl.workflow.query.dto;

// Canonical definition lives in definition.dto.node — re-export so existing query-layer
// imports compile without changes.
public class ActionDefinitionDto extends com.sttl.workflow.definition.dto.node.ActionDefinitionDto {
    public ActionDefinitionDto() { super(); }
}
